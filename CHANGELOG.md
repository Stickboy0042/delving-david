# Changelog

## 2026-05-31

- HTML5 build: published a fresh web build under `html/latest` and refreshed `Delving_David_HTML5.zip` from source commit `65c30bb`. Verification: built with `source/Tools/build_game.sh html5`, validated the archive with `unzip -t`, served it locally, and captured a current smoke screenshot.
- Screenshot: refreshed `latest-build-screenshot.png`; the controls panel now reflects the removed Space/Jump behavior.

## 2026-05-30

- HTML5 build: published a fresh web build under `html/latest` and refreshed `Delving_David_HTML5.zip` from the current local GameMaker source tree. Verification: built with `source/Tools/build_game.sh html5`, validated the zip, and served `index.html` locally with HTTP 200.
- HTML5 build: published a fresh web build under `html/latest`. Verification: served the build locally and confirmed `index.html` and the generated game JavaScript returned HTTP 200.
